find_package(Python3 COMPONENTS Interpreter REQUIRED)
add_executable(topology topology.cpp ../src/game/topology.cpp)
target_compile_features(topology PRIVATE cxx_std_20)
target_include_directories(topology PRIVATE ../src)
add_test(NAME topology COMMAND topology)
add_executable(smoothing smoothing.cpp ../src/game/smoothing.cpp)
target_compile_features(smoothing PRIVATE cxx_std_20)
target_include_directories(smoothing PRIVATE ../src)
add_test(NAME smoothing COMMAND smoothing)
add_executable(bmd_rebuild bmd_rebuild.cpp ../src/game/bmd_rebuild.cpp)
target_compile_features(bmd_rebuild PRIVATE cxx_std_20)
target_include_directories(bmd_rebuild PRIVATE ../src)
add_test(NAME bmd_rebuild COMMAND bmd_rebuild)
add_executable(grade_math grade_math.cpp ../src/config/grade.cpp)
target_compile_features(grade_math PRIVATE cxx_std_20)
target_include_directories(grade_math PRIVATE ../src)
add_test(NAME grade_math COMMAND grade_math)
add_executable(presets_roundtrip presets_roundtrip.cpp ../src/config/presets.cpp)
target_compile_features(presets_roundtrip PRIVATE cxx_std_20)
target_include_directories(presets_roundtrip PRIVATE ../src)
add_test(NAME presets_roundtrip COMMAND presets_roundtrip)
add_executable(visual_parameters visual_parameters.cpp ../src/config/visual.cpp)
target_compile_features(visual_parameters PRIVATE cxx_std_20)
target_include_directories(visual_parameters PRIVATE ../src)
add_test(NAME visual_parameters COMMAND visual_parameters)
add_executable(twilight_profile twilight_profile.cpp ../src/game/twilight.cpp ../src/config/grade.cpp)
target_compile_features(twilight_profile PRIVATE cxx_std_20)
target_include_directories(twilight_profile PRIVATE ../src)
add_test(NAME twilight_profile COMMAND twilight_profile)
add_executable(camera_fov camera_fov.cpp ../src/game/camera_fov.cpp)
target_compile_features(camera_fov PRIVATE cxx_std_20)
target_include_directories(camera_fov PRIVATE ../src)
add_test(NAME camera_fov COMMAND camera_fov)
if(MIDNAFX_BUILD_MOD)
    add_executable(shader_compile shader_compile.cpp)
    target_compile_features(shader_compile PRIVATE cxx_std_20)
    target_include_directories(shader_compile PRIVATE "${CMAKE_BINARY_DIR}/generated")
    target_link_libraries(shader_compile PRIVATE dawn::webgpu_dawn)
    if(WIN32)
        add_custom_command(TARGET shader_compile POST_BUILD COMMAND "${CMAKE_COMMAND}" -E copy_if_different
            "$<TARGET_FILE:dawn::webgpu_dawn>" "$<TARGET_FILE_DIR:shader_compile>")
    endif()
    add_test(NAME shader_compile COMMAND shader_compile)
    set_tests_properties(shader_compile PROPERTIES SKIP_RETURN_CODE 77)
    add_test(NAME package_contract COMMAND "${Python3_EXECUTABLE}"
        "${CMAKE_CURRENT_SOURCE_DIR}/package_contract.py"
        "${CMAKE_BINARY_DIR}/mods/midnafx.dusk")
endif()
