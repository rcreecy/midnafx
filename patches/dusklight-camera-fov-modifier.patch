diff --git a/include/d/d_camera.h b/include/d/d_camera.h
index 6c95d1c..2f38522 100644
--- a/include/d/d_camera.h
+++ b/include/d/d_camera.h
@@ -1094,6 +1094,7 @@ public:
     f32 TrimHeight() { return mTrimHeight; }
     int Type() { return mCurType; }
     int Mode() { return mCurMode; }
+    int Algorithm() { return mCamParam.Algorythmn(mCamStyle); }
     f32 Fovy() { return mFovy + mShake.field_0x3c; }
     bool isModeOK() { return mStyleSettle.mFinished; }
     bool push_any_key() { return mCameraInputActive; }
diff --git a/sdk/include/mods/svc/camera.h b/sdk/include/mods/svc/camera.h
index a901c2b..e5c3bd5 100644
--- a/sdk/include/mods/svc/camera.h
+++ b/sdk/include/mods/svc/camera.h
@@ -8,7 +8,7 @@
 
 #define CAMERA_SERVICE_ID DUSKLIGHT_SERVICE_ID_PREFIX "camera"
 #define CAMERA_SERVICE_MAJOR 1u
-#define CAMERA_SERVICE_MINOR 1u
+#define CAMERA_SERVICE_MINOR 4u
 
 /*
  * Snapshot of a game camera for the frame currently being recorded.
@@ -46,6 +46,14 @@ typedef struct CameraInfo {
 
 #define CAMERA_INFO_INIT {sizeof(CameraInfo)}
 
+typedef struct CameraTargetInfo {
+    uint32_t struct_size;
+    float center[3];       /* rendered look-at target in world space */
+    float focus_distance;  /* positive axial view-space distance to center */
+} CameraTargetInfo;
+
+#define CAMERA_TARGET_INFO_INIT {sizeof(CameraTargetInfo)}
+
 /* 0 is never a valid handle. */
 typedef uint64_t CameraOperatorHandle;
 
@@ -77,6 +85,83 @@ typedef struct CameraOperatorDesc {
 
 #define CAMERA_OPERATOR_DESC_INIT {sizeof(CameraOperatorDesc), NULL, 0, NULL, NULL}
 
+/* 0 is never a valid handle. */
+typedef uint64_t CameraFovModifierHandle;
+
+typedef enum CameraFovContextFlag {
+    CAMERA_FOV_CONTEXT_ACTIVE = 1u << 0,
+    CAMERA_FOV_CONTEXT_NORMAL_MODE = 1u << 1,
+    /* An authored event, conversation, item-get, or demo owns camera presentation. */
+    CAMERA_FOV_CONTEXT_DEMO = 1u << 2,
+    CAMERA_FOV_CONTEXT_DETACHED = 1u << 3,
+    CAMERA_FOV_CONTEXT_CAN_MODIFY = 1u << 4,
+} CameraFovContextFlag;
+
+typedef struct CameraFovModifierState {
+    uint32_t struct_size;
+
+    /* Host inputs. CAN_MODIFY is limited to active mode-0 native chase-camera output. */
+    uint64_t frame_counter;
+    uint64_t ticks;
+    float aspect;
+    float native_fovy;
+    int32_t camera_type;
+    int32_t camera_mode;
+    uint32_t context_flags;
+
+    /* Callback output. The host clamps finite values to 10-120 degrees. */
+    float fovy;
+} CameraFovModifierState;
+
+/* Return true to request state.fovy for this frame. Game thread only. */
+typedef bool (*CameraModifyFovFn)(
+    ModContext* ctx, CameraFovModifierState* state, void* user_data);
+
+typedef struct CameraFovModifierDesc {
+    uint32_t struct_size;
+    const char* debug_name;
+    int32_t priority;
+    CameraModifyFovFn modify;
+    void* user_data;
+} CameraFovModifierDesc;
+
+#define CAMERA_FOV_MODIFIER_DESC_INIT \
+    {sizeof(CameraFovModifierDesc), NULL, 0, NULL, NULL}
+
+/* 0 is never a valid handle. */
+typedef uint64_t CameraChaseModifierHandle;
+
+typedef struct CameraChaseModifierState {
+    uint32_t struct_size;
+
+    /* Host inputs from the native chase controller. */
+    uint64_t frame_counter;
+    uint64_t ticks;
+    int32_t camera_type;
+    int32_t camera_mode;
+    uint32_t context_flags;
+    float native_latitude_far_degrees;
+    float native_latitude_near_degrees;
+
+    /* Callback output. The host clamps this offset to -15 through 15 degrees. */
+    float latitude_offset_degrees;
+} CameraChaseModifierState;
+
+/* Return true to request the latitude offset for this frame. Game thread only. */
+typedef bool (*CameraModifyChaseFn)(
+    ModContext* ctx, CameraChaseModifierState* state, void* user_data);
+
+typedef struct CameraChaseModifierDesc {
+    uint32_t struct_size;
+    const char* debug_name;
+    int32_t priority;
+    CameraModifyChaseFn modify;
+    void* user_data;
+} CameraChaseModifierDesc;
+
+#define CAMERA_CHASE_MODIFIER_DESC_INIT \
+    {sizeof(CameraChaseModifierDesc), NULL, 0, NULL, NULL}
+
 typedef struct CameraService {
     ServiceHeader header;
 
@@ -97,6 +182,41 @@ typedef struct CameraService {
     ModResult (*register_camera_operator)(
         ModContext* ctx, const CameraOperatorDesc* desc, CameraOperatorHandle* out_handle);
     ModResult (*unregister_camera_operator)(ModContext* ctx, CameraOperatorHandle handle);
+
+    /* Minor version 2 */
+
+    /*
+     * Register a post-controller vertical-FOV modifier. The current camera result (native or an
+     * accepted full-camera operator), wide-screen correction, position, target, bank, and
+     * near/far are finalized first. Modifiers run by descending priority, then registration
+     * order, and the first accepted finite result wins. The host ignores output unless
+     * CAMERA_FOV_CONTEXT_CAN_MODIFY is set. The selected FOV is recorded by frame interpolation
+     * and used by camera_draw.
+     */
+    ModResult (*register_fov_modifier)(
+        ModContext* ctx, const CameraFovModifierDesc* desc, CameraFovModifierHandle* out_handle);
+    ModResult (*unregister_fov_modifier)(ModContext* ctx, CameraFovModifierHandle handle);
+
+    /* Minor version 3 */
+
+    /*
+     * Register a native chase-controller latitude modifier. This runs after the game selects
+     * contextual near/far latitude targets and before native smoothing, eye construction, and
+     * collision correction. The host accepts output only for active mode-0 gameplay outside
+     * demos and detached-camera use. Modifiers use the same ordering rules as FOV modifiers.
+     */
+    ModResult (*register_chase_modifier)(ModContext* ctx, const CameraChaseModifierDesc* desc,
+                                        CameraChaseModifierHandle* out_handle);
+    ModResult (*unregister_chase_modifier)(ModContext* ctx, CameraChaseModifierHandle handle);
+
+    /* Minor version 4 */
+
+    /*
+     * Snapshots the rendered camera look-at target and its axial focus distance. game_view has
+     * the same ownership and thread restrictions as get_camera.
+     */
+    ModResult (*get_camera_target)(
+        ModContext* ctx, const void* game_view, CameraTargetInfo* out_info);
 } CameraService;
 
 MOD_DECLARE_SERVICE(
diff --git a/src/d/d_camera.cpp b/src/d/d_camera.cpp
index fe3bb91..8df46f0 100644
--- a/src/d/d_camera.cpp
+++ b/src/d/d_camera.cpp
@@ -3952,6 +3952,11 @@ bool dCamera_c::chaseCamera(s32 param_0) {
         }
     }
 
+#ifdef TARGET_PC
+    dusk::mods::camera_run_chase_modifiers(this, &val12, &val13,
+        dDemo_c::getCamera() != NULL || dComIfGp_event_runCheck(), dusk::isCameraDetached());
+#endif
+
     cXyz player_attention_pos = attentionPos(mpPlayerActor);
     f32 sp1CC;
     if (lineBGCheck(&player_attention_pos, &mViewCache.mEye, 0x40b7)) {
@@ -11401,6 +11406,8 @@ static int camera_execute(camera_process_class* i_this) {
 
 #ifdef TARGET_PC
     widezoom_correction(i_this, i_this->mCamera.TrimHeight());
+    dusk::mods::camera_run_fov_modifiers(&i_this->mCamera, &i_this->view,
+        dDemo_c::getCamera() != NULL || dComIfGp_event_runCheck(), dusk::isCameraDetached());
 
     if (dusk::interp::is_enabled()) {
         dusk::interp::add_interpolation_callback([](void* pUserWork) {
diff --git a/src/dusk/camera_operators.hpp b/src/dusk/camera_operators.hpp
index c4ba650..b4c1e4a 100644
--- a/src/dusk/camera_operators.hpp
+++ b/src/dusk/camera_operators.hpp
@@ -1,9 +1,14 @@
 #pragma once
 
 class dCamera_c;
+struct view_class;
 
 namespace dusk::mods {
 
 bool camera_run_operators(dCamera_c* camera);
+bool camera_run_fov_modifiers(
+    dCamera_c* camera, view_class* view, bool demoActive, bool detached);
+bool camera_run_chase_modifiers(dCamera_c* camera, float* latitudeFar, float* latitudeNear,
+    bool demoActive, bool detached);
 
 }  // namespace dusk::mods
diff --git a/src/dusk/mods/svc/camera.cpp b/src/dusk/mods/svc/camera.cpp
index b93ea00..a243a3b 100644
--- a/src/dusk/mods/svc/camera.cpp
+++ b/src/dusk/mods/svc/camera.cpp
@@ -14,6 +14,7 @@
 
 #include <algorithm>
 #include <aurora/gfx.hpp>
+#include <cmath>
 #include <cstring>
 #include <exception>
 #include <fmt/format.h>
@@ -128,6 +129,38 @@ ModResult camera_get_camera_from_view(
     return snapshot_view(static_cast<const view_class*>(gameView), outInfo);
 }
 
+ModResult camera_get_target_from_view(
+    ModContext* context, const void* gameView, CameraTargetInfo* outInfo) {
+    if (outInfo == nullptr || outInfo->struct_size < sizeof(CameraTargetInfo) ||
+        mod_from_context(context) == nullptr || gameView == nullptr)
+    {
+        return MOD_INVALID_ARGUMENT;
+    }
+    const auto* view = static_cast<const view_class*>(gameView);
+    if (!(view->near_ > 0.0f) || !(view->far_ > view->near_) || !(view->fovy > 0.0f) ||
+        view->fovy >= 180.0f || !(view->aspect > 0.0f))
+    {
+        return MOD_UNAVAILABLE;
+    }
+    const auto& center = view->lookat.center;
+    const float viewZ = view->viewMtx[2][0] * center.x + view->viewMtx[2][1] * center.y +
+                        view->viewMtx[2][2] * center.z + view->viewMtx[2][3];
+    const float focusDistance = -viewZ;
+    if (!std::isfinite(center.x) || !std::isfinite(center.y) || !std::isfinite(center.z) ||
+        !std::isfinite(focusDistance) || focusDistance <= 0.0f)
+    {
+        return MOD_UNAVAILABLE;
+    }
+    const uint32_t structSize = outInfo->struct_size;
+    std::memset(outInfo, 0, sizeof(CameraTargetInfo));
+    outInfo->struct_size = structSize;
+    outInfo->center[0] = center.x;
+    outInfo->center[1] = center.y;
+    outInfo->center[2] = center.z;
+    outInfo->focus_distance = focusDistance;
+    return MOD_OK;
+}
+
 struct CameraOperator {
     std::string debugName;
     int32_t priority = 0;
@@ -139,6 +172,28 @@ struct CameraOperator {
 SlotMap<CameraOperator> sOperators;
 uint64_t sNextOperatorSequence = 0;
 
+struct CameraFovModifier {
+    std::string debugName;
+    int32_t priority = 0;
+    uint64_t sequence = 0;
+    CameraModifyFovFn modify = nullptr;
+    void* userData = nullptr;
+};
+
+SlotMap<CameraFovModifier> sFovModifiers;
+uint64_t sNextFovModifierSequence = 0;
+
+struct CameraChaseModifier {
+    std::string debugName;
+    int32_t priority = 0;
+    uint64_t sequence = 0;
+    CameraModifyChaseFn modify = nullptr;
+    void* userData = nullptr;
+};
+
+SlotMap<CameraChaseModifier> sChaseModifiers;
+uint64_t sNextChaseModifierSequence = 0;
+
 ModResult camera_register_operator(
     ModContext* context, const CameraOperatorDesc* desc, CameraOperatorHandle* outHandle) {
     if (outHandle != nullptr) {
@@ -171,6 +226,94 @@ ModResult camera_unregister_operator(ModContext* context, CameraOperatorHandle h
     return sOperators.erase_owned(handle, *mod) ? MOD_OK : MOD_INVALID_ARGUMENT;
 }
 
+ModResult camera_register_fov_modifier(ModContext* context, const CameraFovModifierDesc* desc,
+    CameraFovModifierHandle* outHandle) {
+    if (outHandle != nullptr) {
+        *outHandle = 0;
+    }
+
+    auto* mod = mod_from_context(context);
+    if (mod == nullptr || desc == nullptr || desc->struct_size < sizeof(CameraFovModifierDesc) ||
+        desc->debug_name == nullptr || desc->debug_name[0] == '\0' || desc->modify == nullptr ||
+        outHandle == nullptr)
+    {
+        return MOD_INVALID_ARGUMENT;
+    }
+
+    *outHandle = sFovModifiers.emplace(*mod, CameraFovModifier{
+                                                .debugName = desc->debug_name,
+                                                .priority = desc->priority,
+                                                .sequence = sNextFovModifierSequence++,
+                                                .modify = desc->modify,
+                                                .userData = desc->user_data,
+                                            });
+    return MOD_OK;
+}
+
+ModResult camera_unregister_fov_modifier(ModContext* context, CameraFovModifierHandle handle) {
+    auto* mod = mod_from_context(context);
+    if (mod == nullptr || handle == 0) {
+        return MOD_INVALID_ARGUMENT;
+    }
+    return sFovModifiers.erase_owned(handle, *mod) ? MOD_OK : MOD_INVALID_ARGUMENT;
+}
+
+ModResult camera_register_chase_modifier(ModContext* context, const CameraChaseModifierDesc* desc,
+    CameraChaseModifierHandle* outHandle) {
+    if (outHandle != nullptr) {
+        *outHandle = 0;
+    }
+
+    auto* mod = mod_from_context(context);
+    if (mod == nullptr || desc == nullptr || desc->struct_size < sizeof(CameraChaseModifierDesc) ||
+        desc->debug_name == nullptr || desc->debug_name[0] == '\0' || desc->modify == nullptr ||
+        outHandle == nullptr)
+    {
+        return MOD_INVALID_ARGUMENT;
+    }
+
+    *outHandle = sChaseModifiers.emplace(*mod, CameraChaseModifier{
+                                                  .debugName = desc->debug_name,
+                                                  .priority = desc->priority,
+                                                  .sequence = sNextChaseModifierSequence++,
+                                                  .modify = desc->modify,
+                                                  .userData = desc->user_data,
+                                              });
+    return MOD_OK;
+}
+
+ModResult camera_unregister_chase_modifier(ModContext* context, CameraChaseModifierHandle handle) {
+    auto* mod = mod_from_context(context);
+    if (mod == nullptr || handle == 0) {
+        return MOD_INVALID_ARGUMENT;
+    }
+    return sChaseModifiers.erase_owned(handle, *mod) ? MOD_OK : MOD_INVALID_ARGUMENT;
+}
+
+uint32_t camera_context_flags(
+    dCamera_c& camera, const bool demoActive, const bool detached) {
+    uint32_t flags = 0;
+    if (camera.Active()) {
+        flags |= CAMERA_FOV_CONTEXT_ACTIVE;
+    }
+    if (camera.Mode() == 0) {
+        flags |= CAMERA_FOV_CONTEXT_NORMAL_MODE;
+    }
+    if (demoActive) {
+        flags |= CAMERA_FOV_CONTEXT_DEMO;
+    }
+    if (detached) {
+        flags |= CAMERA_FOV_CONTEXT_DETACHED;
+    }
+    // The same numeric mode is reused by fixed, ride, event, and other camera styles.
+    // Restrict modification to engine slot 1, the native chase controller.
+    if (camera.Active() && camera.Mode() == 0 && camera.Algorithm() == 1 && !demoActive &&
+        !detached) {
+        flags |= CAMERA_FOV_CONTEXT_CAN_MODIFY;
+    }
+    return flags;
+}
+
 CameraOperatorState make_operator_state(const dCamera_c& camera) {
     return CameraOperatorState{
         .struct_size = sizeof(CameraOperatorState),
@@ -238,8 +381,151 @@ bool run_operators(dCamera_c* camera) {
     return false;
 }
 
+bool run_fov_modifiers(
+    dCamera_c* camera, view_class* view, const bool demoActive, const bool detached) {
+    auto* mainCamera = dComIfGp_getCamera(0);
+    if (camera == nullptr || view == nullptr || mainCamera == nullptr ||
+        &mainCamera->mCamera != camera || !std::isfinite(view->fovy))
+    {
+        return false;
+    }
+
+    const uint32_t flags = camera_context_flags(*camera, demoActive, detached);
+
+    struct PendingModifier {
+        CameraFovModifierHandle handle;
+        CameraFovModifier value;
+        LoadedMod* owner;
+    };
+    std::vector<PendingModifier> modifiers;
+    sFovModifiers.for_each([&](CameraFovModifierHandle handle, const auto& entry) {
+        modifiers.push_back({.handle = handle, .value = entry.value, .owner = entry.owner});
+    });
+    std::ranges::sort(modifiers, [](const PendingModifier& lhs, const PendingModifier& rhs) {
+        if (lhs.value.priority != rhs.value.priority) {
+            return lhs.value.priority > rhs.value.priority;
+        }
+        return lhs.value.sequence < rhs.value.sequence;
+    });
+
+    const CameraFovModifierState initialState{
+        .struct_size = sizeof(CameraFovModifierState),
+        .frame_counter = camera->mFrameCounter,
+        .ticks = camera->mTicks,
+        .aspect = view->aspect,
+        .native_fovy = view->fovy,
+        .camera_type = camera->Type(),
+        .camera_mode = camera->Mode(),
+        .context_flags = flags,
+        .fovy = view->fovy,
+    };
+    for (const auto& entry : modifiers) {
+        if (!entry.owner->active || sFovModifiers.find(entry.handle) == nullptr) {
+            continue;
+        }
+
+        auto state = initialState;
+        bool useState = false;
+        try {
+            useState = entry.value.modify(
+                entry.owner->context.get(), &state, entry.value.userData);
+        } catch (const std::exception& e) {
+            fail_mod(*entry.owner, MOD_ERROR,
+                fmt::format("exception in camera FOV modifier '{}': {}",
+                    entry.value.debugName, e.what()));
+        } catch (...) {
+            fail_mod(*entry.owner, MOD_ERROR,
+                fmt::format("unknown exception in camera FOV modifier '{}'",
+                    entry.value.debugName));
+        }
+
+        if (!useState || (flags & CAMERA_FOV_CONTEXT_CAN_MODIFY) == 0 ||
+            !std::isfinite(state.fovy))
+        {
+            continue;
+        }
+        view->fovy = std::clamp(state.fovy, 10.0f, 120.0f);
+        return true;
+    }
+    return false;
+}
+
+bool run_chase_modifiers(dCamera_c* camera, float* latitudeFar, float* latitudeNear,
+    const bool demoActive, const bool detached) {
+    auto* mainCamera = dComIfGp_getCamera(0);
+    if (camera == nullptr || latitudeFar == nullptr || latitudeNear == nullptr ||
+        mainCamera == nullptr || &mainCamera->mCamera != camera || !std::isfinite(*latitudeFar) ||
+        !std::isfinite(*latitudeNear))
+    {
+        return false;
+    }
+
+    const uint32_t flags = camera_context_flags(*camera, demoActive, detached);
+    struct PendingModifier {
+        CameraChaseModifierHandle handle;
+        CameraChaseModifier value;
+        LoadedMod* owner;
+    };
+    std::vector<PendingModifier> modifiers;
+    sChaseModifiers.for_each([&](CameraChaseModifierHandle handle, const auto& entry) {
+        modifiers.push_back({.handle = handle, .value = entry.value, .owner = entry.owner});
+    });
+    std::ranges::sort(modifiers, [](const PendingModifier& lhs, const PendingModifier& rhs) {
+        if (lhs.value.priority != rhs.value.priority) {
+            return lhs.value.priority > rhs.value.priority;
+        }
+        return lhs.value.sequence < rhs.value.sequence;
+    });
+
+    const CameraChaseModifierState initialState{
+        .struct_size = sizeof(CameraChaseModifierState),
+        .frame_counter = camera->mFrameCounter,
+        .ticks = camera->mTicks,
+        .camera_type = camera->Type(),
+        .camera_mode = camera->Mode(),
+        .context_flags = flags,
+        .native_latitude_far_degrees = *latitudeFar,
+        .native_latitude_near_degrees = *latitudeNear,
+        .latitude_offset_degrees = 0.0f,
+    };
+    for (const auto& entry : modifiers) {
+        if (!entry.owner->active || sChaseModifiers.find(entry.handle) == nullptr) {
+            continue;
+        }
+
+        auto state = initialState;
+        bool useState = false;
+        try {
+            useState = entry.value.modify(
+                entry.owner->context.get(), &state, entry.value.userData);
+        } catch (const std::exception& e) {
+            fail_mod(*entry.owner, MOD_ERROR,
+                fmt::format("exception in camera chase modifier '{}': {}",
+                    entry.value.debugName, e.what()));
+        } catch (...) {
+            fail_mod(*entry.owner, MOD_ERROR,
+                fmt::format("unknown exception in camera chase modifier '{}'",
+                    entry.value.debugName));
+        }
+
+        if (!useState || (flags & CAMERA_FOV_CONTEXT_CAN_MODIFY) == 0 ||
+            (flags & CAMERA_FOV_CONTEXT_NORMAL_MODE) == 0 ||
+            !std::isfinite(state.latitude_offset_degrees))
+        {
+            continue;
+        }
+        const float offset = std::clamp(state.latitude_offset_degrees, -15.0f, 15.0f);
+        *latitudeFar = std::clamp(*latitudeFar + offset, -80.0f, 80.0f);
+        *latitudeNear = std::clamp(*latitudeNear + offset, -80.0f, 80.0f);
+        return true;
+    }
+    return false;
+}
+
 void camera_mod_detached(LoadedMod& mod) {
     sOperators.erase_all(mod);
+    sFovModifiers.erase_all(mod);
+    sChaseModifiers.erase_all(mod);
 }
 
 constexpr CameraService s_cameraService{
@@ -247,6 +533,11 @@ constexpr CameraService s_cameraService{
     .get_camera = camera_get_camera_from_view,
     .register_camera_operator = camera_register_operator,
     .unregister_camera_operator = camera_unregister_operator,
+    .register_fov_modifier = camera_register_fov_modifier,
+    .unregister_fov_modifier = camera_unregister_fov_modifier,
+    .register_chase_modifier = camera_register_chase_modifier,
+    .unregister_chase_modifier = camera_unregister_chase_modifier,
+    .get_camera_target = camera_get_target_from_view,
 };
 
 }  // namespace
@@ -264,3 +555,13 @@ constinit const ServiceModule g_cameraModule{
 bool dusk::mods::camera_run_operators(dCamera_c* camera) {
     return svc::run_operators(camera);
 }
+
+bool dusk::mods::camera_run_fov_modifiers(
+    dCamera_c* camera, view_class* view, const bool demoActive, const bool detached) {
+    return svc::run_fov_modifiers(camera, view, demoActive, detached);
+}
+
+bool dusk::mods::camera_run_chase_modifiers(dCamera_c* camera, float* latitudeFar,
+    float* latitudeNear, const bool demoActive, const bool detached) {
+    return svc::run_chase_modifiers(camera, latitudeFar, latitudeNear, demoActive, detached);
+}
